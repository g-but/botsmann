import React from 'react';
import ReactDOM from 'react-dom';
import ConsultationForm from './components/ConsultationForm';

ReactDOM.render(
  <React.StrictMode>
    <ConsultationForm />
  </React.StrictMode>,
  document.getElementById('consultationFormContainer')
);
